#!/bin/sh
set -eu

# Git exports GIT_DIR to hooks. react-doctor 0.9 then treats the cwd as the
# repo root and doubles the jessica.black/ prefix on every path, so drop it.
unset GIT_DIR
cd "$(git rev-parse --show-toplevel)/jessica.black" && npm run check
